import { UserType } from '../entities/userType.js';
export type UserMetadata = {
    isPaid: boolean;
    isOnTrial: boolean;
    shouldShowTrialBanner: boolean;
    isAdminUser: boolean;
};
export type UserWithMetadata = UserType & {
    metadata: UserMetadata;
};
