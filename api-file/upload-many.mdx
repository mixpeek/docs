---
title: "Upload Files"
description: "Securely send your files to our server where its' content is extracted, indexed and searchable via /search."
api: "POST /file/upload-many"
---

<ParamField body="static_file_paths" type="array">
    List of file paths to perform a bulk upload.
</ParamField>

<ParamField body="semantics" type="boolean">
    If you want to store the context as well in order to perform semantic search.
</ParamField>

<ParamField body="user_id" type="string">
    Local file upload.
</ParamField>

<ParamField body="static_file_path" type="array">
    Static hosted url of the file.
</ParamField>

<ParamField body="tags" type="array">
    List of "tags" which can be used to influence search results.
</ParamField>




## Note:
- Your files are promptly deleted after its' text is extracted. See the <a href="https://mixpeek.com/security">Security Page</a> to learn more.
- Navigate to the <a href="#">file types</a> page to see which file types we currently support.

<RequestExample>

``` shell Shell
{"foo":"bar"}
```

</RequestExample>

<ResponseExample>

```json Response
{"foo": "bar"}
```

</ResponseExample>
